/**
 * Pingram Testing Utilities
 *
 * Jest-style API for mocking SDK behavior in tests.
 *
 * @example
 * ```typescript
 * import { Pingram, PingramMock } from 'pingram';
 *
 * // Mock all SDK requests
 * PingramMock.mock((request) => {
 *   expect(request.body.name).toBe('Test');
 *   return { id: 'key123', name: 'Test' };
 * });
 *
 * const client = new Pingram({ apiKey: 'test' });
 * await client.keys.createAPIKey(...);
 *
 * // Clear mocks
 * PingramMock.clearMock();
 * ```
 */
export type MockRequest = {
    url: string;
    method: string;
    body?: any;
    headers?: Record<string, string>;
    path: string;
    queryParams?: Record<string, string>;
};
export type MockImplementation = (request: MockRequest) => any | Promise<any>;
/**
 * Pingram Testing utilities
 * Jest-style API for mocking SDK behavior
 */
export declare const PingramMock: {
    /**
     * Set mock implementation for all SDK requests
     * Similar to jest.fn().mockImplementation()
     *
     * @example
     * PingramMock.mock((request) => {
     *   expect(request.body.name).toBe('Test');
     *   return { id: 'key123' };
     * });
     */
    mock(fn: MockImplementation): void;
    /**
     * Clear mock implementation
     * Similar to jest.clearAllMocks()
     */
    clearMock(): void;
    /**
     * Reset mock (alias for clearMock)
     * Similar to jest.resetAllMocks()
     */
    resetMock(): void;
    /**
     * Check if mock is active
     */
    isMocked(): boolean;
};
/**
 * Internal: Get mock implementation
 * @internal
 */
export declare function getMockImplementation(): MockImplementation | null;
/**
 * Internal: Check if mocked
 * @internal
 */
export declare function hasMock(): boolean;
/**
 * Internal: Create mock fetch
 * @internal
 */
export declare function createMockFetch(): typeof fetch;
