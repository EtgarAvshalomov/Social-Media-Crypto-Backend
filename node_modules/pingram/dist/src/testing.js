"use strict";
/* eslint-disable @typescript-eslint/no-explicit-any */
Object.defineProperty(exports, "__esModule", { value: true });
exports.PingramMock = void 0;
exports.getMockImplementation = getMockImplementation;
exports.hasMock = hasMock;
exports.createMockFetch = createMockFetch;
let mockImplementation = null;
/**
 * Pingram Testing utilities
 * Jest-style API for mocking SDK behavior
 */
exports.PingramMock = {
    /**
     * Set mock implementation for all SDK requests
     * Similar to jest.fn().mockImplementation()
     *
     * @example
     * PingramMock.mock((request) => {
     *   expect(request.body.name).toBe('Test');
     *   return { id: 'key123' };
     * });
     */
    mock(fn) {
        mockImplementation = fn;
    },
    /**
     * Clear mock implementation
     * Similar to jest.clearAllMocks()
     */
    clearMock() {
        mockImplementation = null;
    },
    /**
     * Reset mock (alias for clearMock)
     * Similar to jest.resetAllMocks()
     */
    resetMock() {
        mockImplementation = null;
    },
    /**
     * Check if mock is active
     */
    isMocked() {
        return mockImplementation !== null;
    }
};
/**
 * Internal: Get mock implementation
 * @internal
 */
function getMockImplementation() {
    return mockImplementation;
}
/**
 * Internal: Check if mocked
 * @internal
 */
function hasMock() {
    return mockImplementation !== null;
}
/**
 * Internal: Create mock fetch
 * @internal
 */
function createMockFetch() {
    return async (input, init) => {
        const impl = getMockImplementation();
        if (!impl) {
            throw new Error('Mock implementation cleared unexpectedly');
        }
        // Parse request
        const url = typeof input === 'string'
            ? input
            : input instanceof URL
                ? input.href
                : input.url;
        const urlObj = new URL(url);
        const method = init?.method || 'GET';
        const path = urlObj.pathname;
        const body = init?.body ? JSON.parse(init.body) : undefined;
        // Parse headers
        const headers = {};
        if (init?.headers) {
            const headerObj = init.headers;
            if (headerObj.forEach) {
                headerObj.forEach((value, key) => {
                    headers[key] = value;
                });
            }
            else {
                Object.assign(headers, headerObj);
            }
        }
        // Parse query params
        const queryParams = {};
        urlObj.searchParams.forEach((value, key) => {
            queryParams[key] = value;
        });
        // Call mock implementation
        const request = {
            url,
            method,
            body,
            headers,
            path,
            queryParams
        };
        try {
            const result = await impl(request);
            // 204 No Content: handler returned sentinel so SDK gets empty body and status 204
            if (result &&
                result.__noContent === true) {
                return new Response(null, {
                    status: 204
                });
            }
            return new Response(JSON.stringify(result), {
                status: 200,
                headers: { 'Content-Type': 'application/json' }
            });
        }
        catch (error) {
            // Pass through the full error body if available, otherwise create one from message
            const errorBody = error.body || { error: error.message };
            return new Response(JSON.stringify(errorBody), {
                status: error.statusCode || 500,
                headers: { 'Content-Type': 'application/json' }
            });
        }
    };
}
